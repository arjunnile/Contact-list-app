import React from 'react';

const Header = () => {
    return (
        <div className="header-row">
            <div className="logo-title">Evolent Health</div>
        </div>
    );
}

export default Header;